//A compiler directive is an annotation that tells the compiler how to parse the source code.
//#[allow(ctrl+backspace to see the list)] -> Use this command only for the line or function below.
#![allow(unused_variables)] // use ! and put on the top to turn the allowing global at the source code.
type Meters = i32;


fn main() {
    let mile_race_length:Meters = 1600;
    let two_mile_race_length: Meters = 3200;
}
